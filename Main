#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <string>
#include "Classes.h";
using namespace std;

int main(int argc, char* argv[])
{
	Command command1;
	cin >> command1;
	Parser parser(command1);
	parser.Parse(command1);
}
